# Copier ce fichier vers .env et adapter les valeurs pour votre environnement.

DEBUG=True
SECRET_KEY=change-me-in-production
ALLOWED_HOSTS=localhost,127.0.0.1

# URL de connexion PostgreSQL (django-environ / dj-database-url format)
DATABASE_URL=postgres://poc_ventes_user:poc_ventes_pwd@localhost:5432/poc_ventes_db

CORS_ALLOWED_ORIGINS=http://localhost:5173,http://localhost:3000
